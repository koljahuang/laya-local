[project]
name = "laya-local"
version = "0.1.0"
description = "Local FastAPI deployment for the Laya decision model"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi>=0.115,<1",
  "laya==0.3.5",
  "uvicorn[standard]>=0.34,<1",
]

[tool.uv]
package = false
